# Olha Playwright Jenkins

UI automation project using Python, Playwright and pytest.

## Project structure

- `pages/` — Page Object classes
- `tests/` — automated tests
- `utils/` — utility modules
- `requirements.txt` — project dependencies

## Run tests

```bash
pytest -v
